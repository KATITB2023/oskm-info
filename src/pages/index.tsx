import { type NextPage } from 'next';
import Layout from '~/layout';

const Home: NextPage = () => {
  return (
    <Layout>
      {/* jumbotron */}
      {/* timleine */}
      {/* sponsor */}
      {/* medpar */}
    </Layout>
  );
};

export default Home;
